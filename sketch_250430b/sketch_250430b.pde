void setup() {
  size(400, 200);         // ウィンドウサイズを幅400、高さ200に設定
  rectMode(CENTER);       // 四角形の描画モードを中心基準に設定
}

void draw() {
  background(255);        // 背景を白にリセット（毎フレーム）

  // ----------- 最初の図形の変換と描画 -----------
  translate(100, 100);             // 座標系の原点を (100, 100) に移動
  rotate(radians(frameCount));     // 毎フレーム右回りに回転
  fill(255, 0, 0);                 // 塗りつぶし色を赤に設定
  rect(0, 0, 50, 50);              // 原点に50x50の四角形を描画（赤い四角）

  // ----------- 2つ目の図形の変換と描画（問題が起きる） -----------
  translate(200, 0);               // 本来は右に200移動したいが、すでに回転しているので方向がズレる
  rotate(radians(-frameCount));    // 回転を打ち消すつもりだが、実際には回転が「累積」する
  fill(0, 0, 255);                 // 塗りつぶし色を青に設定
  rect(0, 0, 50, 50);              // 描画位置・角度が崩れてしまう（青い四角）
}
